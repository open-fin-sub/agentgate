# A–I 可用性整改实施记录

本轮 Goal 尚未完成。基线 `9686d59`，开发分支 `codex/web-productization`；只修改 Web、前端合同类型/映射与验证文档，不改后端合同。台账是 [usability-audit.md](usability-audit.md)，全站完成仍需逐页 §6 与场景 A–G 验收。

## 第一批：A 结构化输入与已定范围变更

采用现有 Vue、Element Plus、tokens 和 API，没有另建前端。新增共享 MessageInput、KeyValueEditor、JsonValueInput、PayloadInput、RuleBuilder、SchemaBuilder、FormSection、JsonFallback、ValueView、ExpectationSummary；MetadataGroup/TokenUsage 用于用量与完整预期摘要。其他已建立模式组件在后续类别逐步迁移，不以“文件存在”宣称落地。

- 真实用例输入采用消息与变量行；预期完整覆盖现有六类合同。删除将流程/工具/策略压平的有损转换，保存时保留检查编号、名称、顺序和复杂条件。没有增加 API 字段或兼容别名。
- Mock 变量、规则与试评改结构化表单；导入以选文件、字段映射、校验为主，粘贴内容进入高级折叠。
- 证据采用可读值和预期摘要，原始快照只通过 JsonFallback 展开；未采集与零值区分。可选分类、变量、样本范围、执行参数收进共享分区。
- 新评估器目录读取摘要后按精确版本读取配置。复用历史配置时比较实体、版本、内容哈希，避免误用已变化的标准。
- 已定变更 RC-01：移除货币字段/计算/指标/阈值，改输入、输出、总 Token 与耗时。Mock 判定使用平均单例 Token；真实应用只展示实际采集值与范围，未提供整体用量时不伪造。
- 已定变更 RC-02：自动生成入口隐藏，旧链接降级为手工创建；生成源模块留待后续。风险验证手工填写并保留来源。自动生成旧 UI 测试另存任务产物，当前验收换为手工多轮发布 v1、文件导入及旧入口降级。

验证：typecheck、build 通过；结构化编辑 4 项，Mock 准备流程桌面/手机 16 项，对比规则 15 项，真实发布修订桌面/手机 2 项，完整六类预期保留 2 项，真实对比证据/Judge 缺失与零值 4 项通过。真实全回归首次 14/18，发现证据预期类型遗漏与旧 Token 文案断言；修复后受影响 4 项复验通过。**全站最终 `npm run test:e2e`、全部逐页清单及 A–G 验收仍待执行，不能宣称整体通过。**

截图：任务产物 `usability-goal/before` 保留改前10张；`checkpoint-a` 保存中间10张，P08 手机、改前桌面已人工查看；关键页桌面/窄屏整页横向溢出均为 false。最终仍需重新生成并逐张查看改后图。

台账：初始441候选 + 人工补查18 =459；本批75已验证、19非缺陷、2后续范围、363待整改。初始扫描把 `concurrency` 中 `currency` 子串及“生成本次”的“成本”子串误命中，保留依据，不冒充修复。RunPage六个并列筛选不是创建表单，不强行折叠。

## 补充 assumptions

- AS-06：Token Mock 默认门槛为平均单例2000 Token，仍是可编辑体验规则，不代表生产政策。真实门禁合同不新增用量条件。
- AS-07：Mock 格式升级为 v2，使用独立浏览器存储键；旧v1原样保留，不迁移、不删除、不把旧货币字段带入新模型。真实数据库不受影响。
- AS-08：业务输入里的贷款申请金额属于被测业务字段，与平台调用成本不同，保留原输入与预期。
- AS-09：新验证环境使用 Web15473/API18473、Redis19379数据库2、独立 `usability-ui.db`，源码来自当前工作树；旧进程、旧数据库保留。截图 before 来自变更前环境，阶段/最终验证使用新环境。

## 提交与未提交工作保护

首次模式提交需同时纳入本任务先前尚未提交的 Web 产品化基础（路由、布局、页面、Mock、API 与依赖），否则共享模式引用的页面与布局无法在检出后构建。这些均在当前 Web 任务范围。AGENTS.md、用户原始旅程/评审/规则、architecture.md、refactor-implementation-plan.md 与其他未认领文档留在工作树，不纳入提交。后续模式在此基础上按类别独立提交。严禁推送、PR、合并。

## G 类：任务文案与状态反馈（本批验证通过）

设计：复用 A 类已建立的 StatusNotice/EmptyState。状态提示保留原条件、错误/警告语义和动作；空状态按页面任务分别给出下一步，不以泛化“无数据”替代。真实请求错误通过 userError 区分权限、失效引用、冲突、输入错误与服务不可用；文件导入保留逐行校验信息。模拟工作区保留统一模拟数据标识，删除分散的架构、路线图说明。

迁移：99处静态状态提示加1处动态提示；37处页面空态，加测评集列表、用例列表、检查记录和关联记录等共享调用。新增人工审计29条（主要为规则与导入校验的技术语言），台账合计488条。原G类生成说明随已定范围缩减列后续范围，入口不可达，不假称已整改隐藏向导。

后端事实：9686d59 已具备评估器目录/草稿/发布及 Skill 静态分析/报告/发现项复核接口；能力页不再将这些列为“后端没有实现”。真实 Web 管理授权、外部目标目录、执行环境与界面接入单列联合工作项。真实页面保留只读目录和报告证据入口，管理与分析写入口隐藏，不在前端假设用户有权限。

验证：typecheck、build通过；真实应用20/20通过。体验区首次68/72，失败为被替换文案的旧断言（能力标题、导入变量错误、正则错误）；断言改为用户语言后完整72/72通过。G类106项已验证，生成说明1项后续范围；全台账488项中181已验证、19非缺陷、3后续范围、285待整改。最终仍须逐页与场景走查，不能据测试通过把整个Goal标完成。

下一类设计（C）：DetailDrawer负责焦点、关闭与当前筛选顺序切换；现有证据/关联页增加显式上下文输入供抽屉复用，保留独立深链。父报告/对比不卸载，证据切换不更新父筛选；复核、编辑存在未保存输入时，关闭和切换均使用同一离开保护。复杂编辑作为独立任务，返回恢复原列表和证据位置。不建立另一套证据页或通用路由框架。

C类进度：真实报告/对比证据、体验报告/对比/分析证据已接入 DetailDrawer；复核页用同一 guard 保护关闭和切换。首次报告保留/不重复读取/焦点返回、复核未保存保护桌面与手机共4项通过。六处真实版本关联入口复用 LineageLink，可在抽屉内继续查看同组版本并返回上层。嵌套关联测试首轮选中了默认展开的测评集分组，已将测试限定在评估器分组；尚待复验。C类未收口，原路由跳转断言需随行为更新。

C类资产详情采用同一原则：对象、测评集、评估器现有页面接收显式实体与查询上下文。共享体验资产入口负责抽屉与筛选顺序，页面自身继续拥有草稿、发布与离开保护；局部版本切换仅更新抽屉上下文，开始另一测评/复制为新资产才进行任务导航。不得复制另一套表单。独立深链及返回要保留源筛选与滚动。

### C 类续验（2026-09-09，尚未收口）

初始静态扫描漏计资产链接与部分关联入口，人工补查29条，加来源返回、全页滚动2条，当前519条，183已验证、34整改中、280待整改、19非缺陷、3后续范围，剩余314。保留原始定位，不以正则消失代替验证。

- 完整回归真实20/22、体验74/76。真实失败源于证据深链混入父报告tab并在修订返回时改变参数顺序；已统一构造，真实旅程8/8复验通过。体验失败为原固定Skill版本的链接断言，更新为默认抽屉、准确版本深链及父URL保留验证后2/2通过。
- 证据全页返回尊重分析/对比来源，校验返回路径，不允许跳往外部地址。返回文案随来源调整。来源返回及抽屉未保存输入保护6/6通过。
- 路由会话保留最近50个页面的滚动位置；异步加载时短暂等待页面高度恢复，用户主动滚动/键盘操作或新的导航会停止等待。布局聚焦主内容不再强制滚到顶部。报告全页证据返回滚动测试桌面/手机2/2通过。
- 本轮 typecheck/build通过（Vite原有大块提示仍存在）。上述复验不能替代最终完整回归和非开发者逐页走查。

后续C类核对重点：复杂编辑返回时的选中项恢复、资产全页来源返回、关联详情中的发布版本入口及筛选顺序。C类34条仍保留整改中，未提交；B/D/E/H/I和最终页面截图、场景A–G继续，不提前宣称完成。

### C 类共享上下文补齐

- `detailState.ts`由应用布局持有，记录详情选择与关联导航引用；真实报告/对比、体验报告/对比/分析/建议及资产入口复用。按规范化的页面路径和查询隔离；当前标签页最多100条。刷新后仍可恢复选择，不存草稿或结果载荷。19个资产入口使用稳定入口标识，避免标题和按钮指向同一资产时同时展开两个抽屉。
- 路由最多保留50个滚动位置，当前标签页刷新后保留。浏览器存储不可用时退回内存。共享抽屉恢复触发按钮焦点并避免焦点导致额外滚动；来源返回路径只允许站内路径。
- 资产全页补上来源返回，保留原查询和所选版本。报告返回恢复原用例；真实修订链接带当前评分标准。关联详情的发布版本改为`DatasetVersionLink`，只读复用`CaseEditor`，读既有精确版本API，不另建编辑器或合同。固定单用例版本验证按真实用例数量禁用前后切换，不强造第二条记录。
- 补查同类切换范围：关联Skill、关联测评集、复合评估器父项、证据评分项使用当前同类列表；单项入口显示单项边界。人工补入来源页发布版本入口后，台账520条：183已验证、35整改中、280待整改、19非缺陷、3后续范围，剩余315。

AS-10：只在当前浏览器标签页保存界面位置与引用选择。草稿仍由既有页面拥有并执行离开保护；后台结果始终重新读取，不把缓存的引用当作结果或权限判断。

复用复核：`goal/p1-demo`的行为基线与`integration/p1-new`的布局分工只作参考；以当前重构的报告、Trace、Dataset版本API及既有CaseEditor为实际复用对象。新增部分仅为共享抽屉导航、引用选择和返回位置，不复制旧主页或建立平行前端。

验证入口已统一为默认`npm run test:e2e`，104项，替换旧Demo测试的覆盖对应关系见[验证范围](usability-test-coverage.md)。本轮完整回归正在运行；恢复关联抽屉后旧测试直接点击底层页面而被遮罩拦截，断言已改为验证恢复后关闭抽屉，再继续下一任务。需用新断言复验，不能把旧失败记成已通过。

仍需扩查独立创建任务与编辑往返：真实测评集工作区的局部选择、创建页往返时的用户配置、列表来源与筛选不能只靠抽屉测试推定通过。C类未收口，后续模式尚未进入实现。

续验结果：默认完整回归102/104；更新关联恢复后的操作断言后，10/10定向复验通过。最新typecheck/build通过。只读版本抽屉另跑桌面/手机2项，验证顶部坐标0、宽度不超视口，并查看两张稳定视口截图；此前fullPage截图会混入固定层之外的滚动区域，不能作为手机抽屉位置证据。只读内容的信息密度、控件视觉仍随B/D统一整改，不据几何通过宣称全站视觉合格。

独立任务返回扩查新增UX-0521～0523，台账523条：183已验证、38整改中、280待整改、19非缺陷、3后续范围，剩余318。具体为真实/体验创建页的固定返回入口、真实测评集→创建/编辑往返的局部选择及填写状态。这些属于前端整改，不标后端缺口。C类保持未收口、未提交；下一步解决这三项并继续核查同模式实例。

### 独立创建/编辑往返整改

新增共享`TaskBackLink`校验站内来源路径并提供统一返回入口，装饰箭头不进入可访问名称。真实和体验创建页复用；对象、测评集、评估器进入创建时携带实际来源。测评集来源包含当前发布版本、用例和列表/编辑视图；共享路由位置记录为该来源保存当前滚动位置。

真实创建页沿用既有浏览器草稿：首次复用历史报告继续核对实际评估器版本/内容，编辑返回用`resume`恢复用户修改过的对象与标准选择，不再次覆盖成历史配置。缺失草稿时明确要求重新核对标准，不编造恢复结果。测评集返回和“用于测评”都采用同一返回规则。体验准备流程沿用原草稿机制，同时保留外层来源。

新增真实往返桌面/手机2项通过：从实际历史任务复用、改对象、取消一项标准、编辑返回后仍保留选择；从测评集创建后返回，原用例和列表视图恢复。体验对象抽屉→创建→筛选列表/原详情返回2项通过。最新typecheck/build通过；完整回归已扩为108项并在运行，首项返回名称测试最初把装饰箭头计入名称而失败，改为aria-hidden后定向2项通过。等待本轮完整结果后收口，台账状态暂不提前关闭。

### B 类准备设计（尚未进入迁移）

199个元数据候选按四类处理：实体名称/类型/版本使用`EntityRef`；分类、难度、数量、时间等独立维度使用`MetadataGroup`；判定与状态保留明确文字和一致含义；标题和主动作聚焦当前任务。先调整共享组件语义，再迁移所有调用，不能把分隔符机械改为空格。

选项中需要多项信息时用带维度标签的选项内容，主选中值保持可读名称；涉及原生控件的统一迁移与D联动。数字0、false、未采集、不适用、无分数不可混同；版本缺失不能自动称为草稿。A/G已移除的货币或文案实例逐条写清改动依据，不冒充本类新修复。报告先给判定，再分维度和证据；只读用例须减少重复标题和填写提示。最终由逐页清单和视觉/业务走查验证，测试通过不替代验收。

实体数据依据：真实报告用manifest里保存的对象、测评集、评估器名称及版本；当前目录用真实目录名称。Mock Run已保存对象和评估器快照，但没有测评集名称快照，展示时可用当前目录识别逻辑测评集，任务版本和用例仍来自该任务。找不到目录项时明确名称缺失，内部编号仅辅助，不新增历史事实。实体详情动作放在独立操作区，避免把可展开的编号区嵌套进按钮或下拉选项。

## C 类收口（2026-09-09）

冻结代码后typecheck、build通过；默认完整回归**108/108通过（7.3分钟，真实26、体验82）**。前一轮107/108中的装饰箭头可访问名称问题已修复，并在本次完整回归中通过。40条C类实例已验证；全台账523条中221已验证、19非缺陷、3后续范围、280待整改。

改前：报告/证据/资产关联以路由跳转为默认，返回时重载并丢失选择；创建页编辑返回会覆盖用户修改的历史配置。改后：共享抽屉/分栏、当前同类顺序切换、离开保护及深链补充；独立任务以站内来源返回，恢复选择、视图与滚动，真实创建编辑往返保留用户选择并重新检查目录版本。

共享实现：DetailDrawer、DetailNavigation、LineageLink、DatasetVersionLink、TaskBackLink及布局持有的detailState；体验引用入口EntityLink和CaseEvidenceDrawer复用这些容器及原页面。没有新增后端合同或业务功能。桌面/手机发布版本抽屉的稳定视口截图已查看，存于任务产物`usability-goal/checkpoint-c`。关键P08/P10/P11/P12最终前后截图仍待全站整改后采集。

下阶段进入B类，继续复用MetadataGroup/EntityRef并完成199个元数据候选核对与迁移。C类通过不代表全部页面完成，25页检查清单及非开发者场景A–G仍保留最终验收门。

## B 类实施中：实体与带标签元数据

C类本地提交为`6f9483d`。B类先修正共享EntityRef：缺失版本不再推定草稿；实体名称支持页面正确的标题层级和表单标签，编号按需展开。MetadataGroup保留0、false和缺失值的区别，异质信息分维度标注。报告、证据、评估器、总览、任务列表/进度、创建、测评集、关联版本和对比页正在迁移；用例卡片的分类、难度、轮次分别标注，实体详情动作不嵌入可展开编号区。

初批真实回归发现旧Judge拼接断言和评估器定位断言需要更新；当时26项运行中止，不能记为全部通过。修正后真实报告/Judge/上下文6项通过。体验资产54项中52项通过、2项为旧英文试评断言；修正后2项复验通过。后续元数据32项中30项通过、2项为旧合并摘要拼接断言；已改为分别检查去重、冲突与无冲突数量，纳入本次完整回归。54项包含纯计算测试，不全是交互旅程。

共享MetadataGroup增加按钮内使用的inline模式（span结构），默认仍用带dt/dd的语义分组，避免按钮内嵌不合法块结构。EntityRef支持正确标题层级和名称插槽；缺失版本显示未提供，明确草稿由调用方传入。元数据/实体选项统一自适应高度，选中值保留可读名称。真实API字段和值未改变。

覆盖真实报告、证据、评估器、总览、列表、创建、测评集、关联与对比；体验对应页面及分析、建议、资源、比较前置核对、版本合并、试评、来源组件复用同一模式。来源资产补用EntityLink，在父页内查看固定版本及同类切换；真实任务来源仍进入独立任务。补查13项B及1项C后，台账537条，B类212项，剩余294项。原始行号保留发现时位置，不以行号移动或分隔符消失自动关闭。

冻结代码typecheck/build通过，默认110项（真实26、体验84）完整回归正在运行。checkpoint-b保存关键页面10张中间截图；已查看报告桌面、证据手机、创建桌面、评估器桌面及额外版本选项桌面/手机截图。选项无重叠，零值可辨；报告标题重复/结论位置、只读控件对比度及证据英文术语仍需分别随B/D/H处理，截图不是最终验收。最终25页与场景A–G验收仍未完成。

### B 类补查与收口复验

首轮冻结代码默认110/110通过（8.2分钟）。人工视觉复核补入两项报告层级B4，台账539条，B类214项、剩余296项。真实报告判定提前到标题下；两个报告的固定对象/输入按需展开，避免首屏重复铺陈。变更摘要用新增/修改/删除分组的EntityRef，保留同名用例辅助编号；复合表单标签改为用户输入和预期表现。补改后typecheck/build通过，受影响46项桌面/手机旅程正在复验。checkpoint-b-final的10张中间截图已采集，报告桌面/手机已查看：结论优先，0与缺失分数不混同。最终术语、控件和全页检查仍未完成。

### D 类设计与复用判断（待B收口后实施）

沿用当前ConfigProvider与tokens/base，不引入第二套控件。provider统一large档（Element Plus 40px），尺寸令牌明确40px；移除页面零散small覆盖。可见文本/选择/数字/时间输入改用Element Plus，选项继续复用EntityRef/MetadataGroup。隐藏的浏览器文件选择器是文件读取机制，只有可见上传界面需要统一；保留文件格式/大小检查及既有读取流程。原生button/链接可保留语义和既有统一ag-button样式，不把控制尺寸整改变成业务逻辑重写。

颜色：保留品牌绿、深色按钮文字；深语义绿用于白底链接。弹层不在product-main内，主题状态必须全局覆盖默认/hover/active/焦点，不能只修页面按钮。辅助文字、状态、边框、只读值单独核验；只读信息不得靠淡灰禁用字段传递唯一内容。焦点环涵盖summary和组件按钮，减少动效覆盖程序滚动与CSS动效。窄屏控件/弹层不溢出，触控目标至少24px。

复用来源：已读三方清单中goal/p1-demo保留输入和导航行为，integration/p1-new仅参考样式与布局分离；实际迁移以当前Element Plus、tokens/base与既有表单验证为准，不复制旧Demo表单或旧路由。D验证包括真实筛选/提交、复核空分数和0、日期输入、键盘/焦点/对比度及全站回归。选项value类型、API契约和草稿保存语义保持原有边界。

## B 类收口

最后补改46/46通过（5.9分钟），覆盖真实创建/报告/版本往返、体验复核/修订、分析补充用例、版本准备和存储恢复。默认完整110/110属于此前冻结版本，最后仅层级/标签/摘要的变化由46项受影响回归验证；未把两次结果冒充同一次全量运行。最新typecheck/build通过。

214个B候选已逐项核对：213关闭为已验证，UX-0308是描述比较限制的叙述句，记录为非缺陷，术语随H优化。数学分式、分页总数、同类标签列表、用户资产名称中的分隔符不作为异质字段展示缺陷；任务生成名称的可读性随H统一。来源上下文新增C项也通过。全台账539条，435已验证、20非缺陷、3后续范围、81未关闭。B收口不代表D可访问性或全站业务验收通过。

落地共享组件为EntityRef（语义标题/名称插槽/版本缺失）、MetadataGroup（默认dt/dd与按钮内inline结构），继续复用TokenUsage、StatusNotice和EntityLink。API字段、版本、数值与业务判断保持原来源；真实页面没有新增假数据。

## D 类实施中

B类本地提交181a44a，剩余81项。ConfigProvider统一large档，实测真实/体验创建页输入、选择、数字及主按钮均40px。移除零散small和原生控件遗留样式，可见文本/选择/数字/预约均复用Element Plus；仅真实导入保留隐藏文件读取input，不是并行可见表单。选择值、数字空值与0、原API契约保持。类型/构建通过；真实26/26复验通过，体验人工分数路径已通过。初轮测试误点组件内部原生input、隐藏选项，已改为键盘选择/空格切换和表格状态定位，不用force绕过。

全局主题扩到Teleport弹层；新增浅色底辅助文字令牌、标签与字数提示对比度；移除折叠标题焦点缺口，支持prefers-reduced-motion及>=24px关闭/清空按钮。只读值采用正文颜色。24个可达页面默认可见文本扫描候选为0（checkpoint-d-final/text-contrast.json）；不含hover、placeholder、图像、伪元素和控件边框，不称完整WCAG验收。RunWorkspacePage未被当前路由引用，只能作为保留源码核查，不编造其运行验收。

AS-11：保留规范原始辅助色#6B7280；在浅灰/浅绿底采用更深#626977，因原色在这些底色实测仅4.39/4.34:1。品牌绿保留，主按钮用深字；输入边界采用可辨识的深辅助色。依据UI审计D-02与AA强制优先，不新增品牌体系。

新增弹层尺寸/对比度/Tab焦点、日期控件本地时间与显式保存草稿恢复4项，初轮断言误用程序focus和textbox角色而失败，按真实键盘和combobox语义修正后复验中；没有为测试新增自动保存。D类42项保持整改中，完整回归、截图和其余状态尚待验证。

D续验：新增4/4通过（21.3秒），包含真实Teleport弹层40px、默认/hover文字>=4.5、Tab焦点、选择框键盘与窄屏宽度，以及体验预约日期输入、显式保存和恢复草稿。最新冻结代码typecheck/build通过；默认完整回归扩为114项，运行中。checkpoint-d关键10图已采集，已查看P10手机、P08桌面、P12桌面；输入更统一、浅底文字可读，证据中的业务英文仍需H整改，不能以颜色通过替代非开发者验收。

### E/H/I 横切设计准备

E复用RoleGate：普通测评人员与只读用户只看资源状态、队列及下一步，新增/启停/测试/公共限制/资源审计按管理员展示；体验角色选择器保留明确的模拟边界。真实应用没有角色身份合同，管理入口保持隐藏，不能本地伪造权限。RoleGate按显式角色判定，未知/空身份默认不放行，不把Mock角色注入真实应用。

H集中词表：测评任务、测评对象、测评集、评估器、用例、执行轨迹与人工复核。业务状态和数值映射仅限明确语义字段和内置目录，原始内容可查，不把任意外部文本或编号猜成中文事实；统一不适用、无分数、跳过与0的表达。人工复核统一确认问题/机器误判/需复核，保留机器原判与复核记录。

I先核对既有确认/离开保护/InlineError。真实与体验发布版本、移除已有轮次/子项存在检查重点：确认在写入前，取消不改草稿；输入错误保留内容并紧邻字段，服务异常使用StatusNotice及可行下一步。现有CaseTable删除、归档/放弃草稿已有确认，不能为可恢复的普通选择额外叠加弹窗。以上仍为设计准备，未在D冻结回归期间实施。

## D 类收口

默认完整回归114/114通过（9.8分钟）。最后仅补齐文字按钮hover语义色，定向4/4通过（21.8秒），最新typecheck/build通过；不把最后补改后的4项冒充再次执行全量。临时浏览器主题探针测得主色/危险/成功文字按钮悬停色为#006652/#922018/#166344，白底对比分别6.95/8.60/7.24，40px。探针只验证主题规则，无业务数据写入；真实交互由回归覆盖。

42个D候选中41已验证，隐藏文件选择器UX-0060核实为非缺陷；不是可见原生与组件混用。台账539条中476已验证、21非缺陷、3后续范围，剩余39项（E/H/I与最终F）。24个可达页面默认文字对比度扫描无候选，已查看关键P10手机/P08桌面/P12桌面及此前报告双端截图。组件交互测试和扫描不是完整业务/视觉验收，25页清单与场景A–G继续。

共享落地：ConfigProvider、tokens的控件尺寸/浅底辅助色/语义文字色、通用焦点与减少动效；实际页面复用Element Plus输入、选择、复选、数字、日期控件，EntityRef/MetadataGroup继续承载选项内容。没有修改后端合同或制造真实应用数据。下一步E先完成资源角色边界，再H术语、I反馈与确认，最后逐页及场景验收。

## E 类实施中

D类本地提交67ab461，剩余39项。RoleGate现按实际editor/viewer/admin角色值匹配，无别名映射，缺失角色默认不放行。资源页普通用户只读名称/模型/可用性/公共限制与等待任务，提供查看测评配置的下一步；凭据掩码、测试/启停、公共限制编辑、管理审计及新增弹窗均按管理员显示。角色切换关闭管理弹窗，异步测试/确认完成后重新检查当前角色。入口统一为资源与队列，普通页标题为资源状态。

真实应用仍没有Mock角色或资源管理入口。这里的RoleGate是前端显示边界，不能替代未来真实后端鉴权；当前不假设服务已返回用户权限。新增桌面/手机角色切换验证2项，默认回归扩为116项，typecheck/build与全量回归正在顺序执行。旧只读用户看到禁用新增按钮的断言改为入口不存在。UX-0360保持整改中，未提前关闭。

## E 收口与批量记账切换

- 改动：RoleGate按角色展示资源操作，本地提交d6dc813。
- 验证：typecheck/build通过；完整回归116/116（10.2分钟）。
- 剩余：冻结539条，477已验证、21非缺陷、3后续范围、38待整改；新增PL-01留F处理。
- 记账：JSON为唯一源，MD纯生成；新收口引用批次，不重复resolution；无新增假设。

## H 批次实施

- 改动：人工复核使用RunSupport集中词表；测评任务命名和人工汇总说明统一。
- 验证：已grep tests并同批更新断言；人工复核双端2/2通过，typecheck/build通过；类末全量运行中。
- 剩余：38条未提前关闭；24文件H/I候选已存usability-hi-candidates.json，PL-02～04留F。
- 新假设：无；源码标识、判定和持久数据未改变。

## H 收口

- 改动提交de2e33d；H-01按文件批量关闭12条，resolution只记一次。
- 验证：定向2/2、typecheck/build、完整116/116（8.5分钟）通过。
- 剩余：冻结539条，剩余26（I25/F1），parking lot 4条；无新增假设。

## I 批次实施

- 改动：复用ElMessageBox确认真实/体验发布、移除对话轮次和子评估器；确认前说明范围与固定版本影响，取消保留草稿。
- 验证：先grep全部受影响断言并同批更新；新增取消/保留/确认行为断言，定向18项运行中。
- 剩余：I25/F1未提前关闭；既有StatusNotice/InlineError/删除确认复用，清空选择核实为可恢复操作。
- 新假设：无；继续使用原API，角色及当前对象在确认后复查。

## I 收口

- 改动提交c0a4f77；I-01按文件批量收口25条，24已验证，清空选择1条非缺陷。
- 验证：定向18/18、typecheck/build、完整116/116（10.1分钟）通过。
- 剩余：冻结539条剩F1，4条parking lot进入F集中处理；无新增假设。

## F 集中处理

- PL-01资源只读摘要复用EntityRef/MetadataGroup，PL-03真实轮次确认复用ElMessageBox；PL-04报告首屏显示对象身份。
- PL-02按13文件模板词表脚本统一中文；结果词复用resultLabels，已知业务值与目录翻译保留原始数据。
- 验证：相关断言同批更新，真实轮次确认/保留与手机资源摘要等定向8项运行中。
- 剩余：F1与4条parking lot待最终验证；新增AS-12记录显示翻译边界。

## F 视觉例外修正

- 定向8/8通过；真实证据按expectation_id关联业务字段，显示译名不改变原始值。
- PL-05创建页改为对象接入条件，能力条目按现有路由更正已具备API。
- PL-06按需CSS加载覆盖33个标签颜色，修正共享选择器优先级并补定向对比度断言。
- 移除例行通过截图，仅F脚本采集关键对照；首批24页无整页横向溢出，最终复查继续。
- 剩余：F1及6条parking lot待最终验证；AS-12外的原始业务内容保留原文。

## F 冻结验收版本

- 实際UI走查D：理由校验→采纳→未关联禁止回归→关联v2→等待完成→有依据的未通过结论，未伪称上线。
- 条件展开选择框补明确可访问名称；混淆矩阵复用EntityRef显示固定技能名称/版本，保留原编号和配对计算。
- 验证：定向8/8、标签/键盘/弹层2/2通过，24页文字对比度候选0、整页横向溢出0。
- 最后typecheck/build/完整回归运行中；F1及6条parking lot尚未关闭，无新增假设。

## F 最终收口

- 代码4e23e36；F-01一次resolution关闭F1及PL-01～06，冻结539条待整改0（514已验证/22非缺陷/3后续范围）。
- 验证：最后typecheck/build、完整116/116（10.5分钟）；F定向8/8+2/2；24页对比度候选0/整页溢出0，最终10图审阅。
- 场景D另完成导出与建议历史回溯；P10基线用原始源码/原API补拍，原截图不覆盖。
- 文档：JSON唯一源，31条联合项及用户降级完整；逐页与A–G仅例外，AS-01～12完整保留，无新增假设。
- 记账脚本幂等/JSON不变/冻结拒写验证通过；保留未认领工作，仅本地提交，无push/PR/合并。
